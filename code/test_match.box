@incl <stdprint>
@incl <stdio>

@incl "incl_test.box"

box _start[] (
    open init_text_mode[]
    open clear_screen[]

    open include_function[]

    num32 x : 1

    match [x] (
        case [1] (
            open print["x is one$"]
        )
        case [2] (
            open print["x is two$"]
        )
        case [5] (
            open print["x is five$"]
        )
        default (
            open print["others$"]
        )
    )

    open trapf[]
)
